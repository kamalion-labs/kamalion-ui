import { FormButtons } from "./FormButtons";
import { FormError } from "./FormError";
import { FormRoot } from "./FormRoot";

export const Form = {
  Root: FormRoot,
  Error: FormError,
  Buttons: FormButtons,
};
